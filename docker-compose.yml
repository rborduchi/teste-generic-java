version: '3'

services:

  postgres:
    image: "postgres:12"
    volumes:
      - db_data:/var/lib/postgresql/data
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: borduchi
      POSTGRES_PASSWORD: borduchi
      POSTGRES_DB: teste_generic

volumes:
  db_data:
